pipeline {
    agent any
    stages {
   		 
   		stages {
        	stage('build') {
            	steps {
                	bat 'mvn --version'
                	bat 'mvn clean compile'
            	}
        	}
    	}
    
    }
}
